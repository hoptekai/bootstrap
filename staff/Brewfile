# Brewfile for the STAFF profile (no Nix — apps only).
#
# Mirrors packages/casks-shared.nix. Keep the two in sync when you add a fleet-wide app.
# Optional picks (Spotify, etc.) are appended below the marker by `boot pick`.

cask "bitwarden"
brew "bitwarden-cli"
cask "google-chrome"
cask "slack"
cask "zed"
cask "claude-code"

# BOOT:OPTIONAL (managed by `boot pick` — safe to also edit by hand)
# BOOT:END
