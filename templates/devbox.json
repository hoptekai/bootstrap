{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/main/.schema/devbox.schema.json",
  "packages": [
    "nodejs@22",
    "python@3.12"
  ],
  "shell": {
    "init_hook": [
      "echo 'devbox shell ready — machine tools come from the bootstrap repo, project tools from here'"
    ],
    "scripts": {
      "test": ["echo 'add your test command'"]
    }
  },
  "//": [
    "Copy this into a new project, then `direnv allow` so the shell auto-activates.",
    "Pull secrets from Bitwarden instead of committing them, e.g.:",
    "  export DATABASE_URL=$(bw get password 'my-project-database-url')",
    "or use bw get notes for multi-line secrets"
  ]
}
